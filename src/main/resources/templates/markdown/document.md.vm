tags:${projectName}--接口文档
---
${hashSymbol2} 目录
> ${hashSymbol3}

#foreach ($module in $moduleList)
* [${module.id}  ${module.moduleName}]
  #foreach ($api in $module.apiList)
	[${module.id}.${api.id}  ${api.apiName}]
  #end
#end
* [测试目录] ${interval}
  #foreach ($api in $apiList)
	[$api.name] (${hashSymbol1}${api.moduleId}_${api.id}) ${interval}
  #end

#foreach ($api in $apiList)
${hashSymbol3} <font id=${api.moduleId}_${api.id}>${api.id}.${api.name}</font>
* url: $api.url
* method: $api.method

${hashSymbol6} 请求参数
field|类型|desc|必填
:---:|:---:|:---:|:---:|
#foreach($request in $api.requestList)
$request.field|$request.type|$request.desc|#if($request.isNullable==1) Y #elseif($request.isNullable==0) N
  #end
#end


${hashSymbol6} 返回参数
field|desc|必填
:---:|:---:|:---:|
#foreach($response in $api.responseList)
$response.field|$response.type|$response.desc
#end

```
${api.responseJson}
```
#end

